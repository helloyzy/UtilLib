(1) Make sure install the maven
(2) To build out a jar file, navigate to the project folder and execute "mvn package", the generated jar file should be under the /target
(3) To install the jar to the native maven repository, execute "mvn install"